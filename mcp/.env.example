# Optional. Public hosted OS tools work without these.
# Gated bootstrap_whoami / bootstrap_list_company_labels need both.
# Use the existing pirin.ai project (supabase-pirin-ai / vsqekesftzstsjvcowgm).
# Never put the service role here. Never commit real keys.

BOOTSTRAP_SUPABASE_URL=https://vsqekesftzstsjvcowgm.supabase.co
BOOTSTRAP_SUPABASE_ANON_KEY=
